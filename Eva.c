#include <stdio.h>

//#
// -Guardar todo en un array
// -Imprimir array.
//#

int main()
{
    int c;
    int index;
    int arr[100];
    index = 0;
    
    while((c = getchar()) != EOF)
    {   
        
			putchar(c);
		
	}
		
}
